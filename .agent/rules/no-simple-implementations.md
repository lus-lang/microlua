---
trigger: always_on
---

When implementing or refactoring something that is complex or significant, do not avoid a full implementation by inserting a "simple"/"simplified" version. Instead, create a task boundary (or even an implementation plan if necessary) with the proper artifacts to plan the development of the complex or significant feature/refactor.
