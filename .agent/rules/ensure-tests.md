---
trigger: always_on
---

When adding new features or functionality to the runtime, OR adjusting existing functionality, locate tests in /tests that already code-covers the modifications. If there aren't any, create new tests, preferably interpreter instead of internal tests.
